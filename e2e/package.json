{
  "name": "cordly-packages-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "The browser, mobile, and server-rendering gates. A separate project so the gate that runs on every push does not install a browser harness it never uses.",
  "license": "MIT",
  "engines": {
    "node": ">=24 <25"
  },
  "packageManager": "npm@11.6.2",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.13.0",
    "@playwright/test": "1.62.1"
  }
}
